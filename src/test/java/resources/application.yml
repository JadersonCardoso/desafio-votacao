server:
  port: 8888
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false
logging:
  level:
    root: info
spring-doc:
  paths-to-match: /api/v1/**
  swagger-ui:
    use-root-path: true